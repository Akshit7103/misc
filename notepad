  - One to check: does the WITH Now Assist flow work in background/batch, or only interactively? The native MID routing
  only applies in interactive sessions — in a flow it goes direct and can't reach Chinou. We solved it with a small
  loopback bridge (connection → instance endpoint → MID-pinned REST → Chinou). Worth reflecting in the Routing row.
  - Verify auth: we found the NASK path needed Basic auth (GCD account) — the Tea Token was rejected; the deck shows
  both flows using the Tea Token.
  - Inputs: add Excel/XLSX (the main attachment type) alongside PDFs/docs.
  - Make the comparison a decision: add licensing (Now Assist needs a licence, direct REST doesn't), governance, and
  portability — and a one-line recommendation of when to use which.
