  Hi Paramjeet,

  Confirming the Compare & Match use case (Reg ID AIUC00336) is now compliant:

  - The Registration ID is included in every Chinou API call — implemented at our shared client layer, so it covers all our
  workflows in one place.
  - Sent in the request body (LLMRequest.parameters), as the Registry docs require — we found a query parameter is ignored.
  - Validated against the live API: calls return successfully and the "Include the Registration ID" warning no longer appears, so
  usage now attributes to AIUC00336.
